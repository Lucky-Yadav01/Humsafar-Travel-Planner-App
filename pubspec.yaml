name: humsafar_p1
description: "A new Flutter project."

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

# Versioning
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0   # ✅ For custom fonts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_native_splash: ^2.3.2  # ✅ (optional splash screen builder)

flutter:
  uses-material-design: true

  assets:
    - assets/images/

  # Optional: Fonts (uncomment if needed)
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: assets/fonts/CustomFont-Regular.ttf
  #       - asset: assets/fonts/CustomFont-Bold.ttf
  #         weight: 700
